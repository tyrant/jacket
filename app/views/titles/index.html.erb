
<% if @titles.length > 0 %>
  <ul>
    <% @titles.each do |title| %>
      <li><%= title.title %></li>
    <% end %>
  </ul>
<% else %>
  No book titles exist. <%= link_to 'Add one!', new_title_path %>
<% end %>
