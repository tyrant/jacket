<%= image_tag @illustration.illustration.url(:large) %>

<% if @illustration.user == current_user %>
  <%= link_to 'Delete', @illustration, :method => 'delete', :confirm => 'Are you sure?' %>
<% end %>
